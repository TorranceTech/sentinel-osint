allow-unwrap-in-tests = true
allow-expect-in-tests = true
# Proper nouns used in docs (".." keeps clippy's defaults).
doc-valid-idents = ["AbuseIPDB", "APIv2", "IPinfo", "MalwareBazaar", "URLhaus", "VirusTotal", ".."]
